name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Крок 1: Завантажуємо код з репозиторію
      - name: Checkout code
        uses: actions/checkout@v3

      # Крок 2: Налаштовуємо Node.js середовище та кешування
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: package-lock.json
      
      # Крок 3: Вимикаємо corepack щоб Next.js не намагався використати yarn замість npm
      - name: Disable corepack
        run: corepack disable

      # Крок 4: Встановлюємо залежності з кореня проєкту
      - name: Install dependencies
        run: npm ci

      # Крок 5: Створюємо білд для веб-додатку
      - name: Build the web app
        run: npx turbo run build --filter=fintrack-web
        env:
          COREPACK_ENABLE_STRICT: "0"

      # Крок 6: Деплой на gh-pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./apps/web/dist
          publish_branch: gh-pages