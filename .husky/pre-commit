#!/usr/bin/env sh
set -e

if command -v pnpm >/dev/null 2>&1; then
  pnpm run lint-staged
elif command -v docker >/dev/null 2>&1 && [ -f "./dx" ]; then
  bash dx run lint-staged
else
  echo "pre-commit: neither pnpm nor Docker+dx is available."
  exit 1
fi
